# python-assistant
Python - reusable functions and modules

Python Variables
- No python command to declare a variable, it is created once you assign a value to it
- Varible names can contain alphanumeric values only (ex. a-z, 0-9 and _). Varibale name cannot start with numeric  value
- Use global keyword to create global varibales
- Multiple varibales can be assigned with different values in a single line